{
  "name": "ayowebku-assist",
  "version": "1.0.0",
  "private": true,
  "description": "Chatbot customer service Ayowebku dengan sumber dari website resmi.",
  "scripts": { "check": "node --check app.js && node --check api/chat.js && node --check api/scrape.js" }
}